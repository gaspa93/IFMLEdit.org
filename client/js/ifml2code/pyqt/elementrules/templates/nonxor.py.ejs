from PyQt4 import QtGui
<% for (var i = 0; i < children.length; i++) { -%>
import <%=children[i].id %> as C<%=i %>
<% }
   for (var i = 0; i < events.length; i++) { -%>
import ../events/<%=events[i].id %> as E<%=i %>
<% } -%>

def setupUi():
    view = QtGui.QWidget()
    view.setObjectName("<%=id %>")

<% for (var i = 0; i < children.length; i++) { -%>
    elem<%=i %> = C<%=i %>.setupElem()
    view.addRow(elem<%=i %>)
<% } -%>

    return view
