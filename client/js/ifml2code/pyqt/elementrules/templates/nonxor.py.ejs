from PyQt4 import QtGui
<% for (var i = 0; i < children.length; i++) { -%>
import <%=children[i].id %> as C<%=i %>
<% }
   for (var i = 0; i < events.length; i++) { -%>
import ../events/<%=events[i].id %> as E<%=i %>
<% } -%>

def setupUi():
    w = QtGui.QWidget()
    w.setGeometry(100,100,200,50)
    w.setObjectName("<%=id %>")

    # content based on children
    # b = QtGui.QLabel(w)
    # b.setText("Text from <%=id %>")
    # b.move(50,20)

    return w
