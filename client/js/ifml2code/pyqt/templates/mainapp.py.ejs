import sys
from PyQt4 import QtGui

# import other windows and actions as functions
<% for (var i = 0; i < children.length; i++) { -%>
import <%=children[i] %> as C<%=i %>
<% } -%>

def mainapplication():

    app = QtGui.QApplication(sys.argv)

    # main window
    mainw = QtGui.QWidget()
    mainw.setGeometry(100,100,700,600)
    mainw.setWindowTitle("Main Application Window")

    # set tab widgest as default
    tabWidget = QtGui.QTabWidget(mainw)
    tabWidget.setGeometry(30, 30, 621, 531)

    # show windows as tab
<% for (var i = 0; i < children.length; i++) {-%>
    w<%=i %> = C<%=i %>.setupUi()
    tabWidget.addTab(w<%=i %>, "<%=children[i] %>")
<% } -%>

    mainw.show()

    # enter the main loop of the application
    sys.exit(app.exec_())
