import sys
from PyQt4 import QtGui

# import other windows and actions as functions
<% for (var i = 0; i < children.length; i++) { -%>
import <%=children[i] %> as C<%=i %>
<% } -%>

def mainapplication():

    app = QtGui.QApplication(sys.argv)

    # show windows
<% for (var i = 0; i < children.length; i++) {-%>
    w<%=i %> = C<%=i %>.widget()
    w<%=i %>.show()
<% } -%>

    # enter the main loop of the application
    sys.exit(app.exec_())
